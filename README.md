# CoolieHub Web

Single-page React + Tailwind app for CoolieHub (Telugu mix).  
Tabs: Home, Farmer, Worker, Insurance, Students, నా యోచన, Receipts.

## Dev
```
npm install
npm run dev
```
## Build
```
npm run build
```
Serve `dist/` on any static host (Vercel/Hostinger).
